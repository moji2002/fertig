---
layout: layout.njk
permalink: "index.html"
title: "fertig \u2014 classless CSS framework, no JavaScript, 8.7 KB"
description: "fertig is a classless CSS framework built only on CSS that every current engine already ships. Style semantic HTML with one link tag: dialogs, popovers and tooltips work with no JavaScript. 8.7 KB gzipped, no build step, MIT."
ogTitle: "fertig \u2014 classless CSS framework, no JavaScript"
ogDescription: "Link one stylesheet and semantic HTML comes out designed. Built only on CSS every current engine ships. 8.7 KB gzipped, no build step, no runtime."
ogImageAlt: "A page styled by fertig: form controls, a data table and cards on a dark background."
canonical: ""
active: index
langs:
  - bash
schema: |
  {
    "@context": "https://schema.org",
    "@type": "SoftwareApplication",
    "name": "fertig",
    "applicationCategory": "DeveloperApplication",
    "description": "A classless CSS framework built only on CSS that every current engine already ships. Semantic HTML in, a designed page out, with no JavaScript and no build step.",
    "url": "https://moji2002.github.io/fertig/",
    "downloadUrl": "https://www.npmjs.com/package/fertig",
    "codeRepository": "https://github.com/moji2002/fertig",
    "softwareVersion": "3.0.0",
    "operatingSystem": "Any",
    "license": "https://opensource.org/licenses/MIT",
    "offers": {
      "@type": "Offer",
      "price": "0",
      "priceCurrency": "USD"
    }
  }
---


<header class="hero hero-center">
  <div class="hero-glow" aria-hidden="true"></div>
  <div class="wrap">
    <a class="pill" href="https://github.com/moji2002/fertig/blob/main/CHANGELOG.md">
      <svg class="icon" aria-hidden="true"><use href="icons.svg#zap"></use></svg>
      v{{ site.version }} — modern defaults
    </a>
    <h1>Modern HTML and CSS.<br><span class="dim">No JavaScript at all.</span></h1>
    <p class="lede">A classless stylesheet built only on CSS that every current
      engine already ships. Dialogs, popovers and tooltips run on native HTML
      attributes — no polyfills, no build step, no runtime. Link
      one file and the page is finished.</p>
    <p class="row cta-row">
      <a class="primary" data-size="lg" role="button" href="docs.html">
        <svg class="icon" aria-hidden="true"><use href="icons.svg#book-open"></use></svg>Get started</a>
      <a role="button" data-size="lg" href="blocks.html">
        <svg class="icon" aria-hidden="true"><use href="icons.svg#blocks"></use></svg>View blocks</a>
    </p>
    <p class="proof">
      <span>8.7 KB gzipped</span>
      <span>0 JavaScript</span>
      <span>0 dependencies</span>
      <span>0 required classes</span>
      <span>MIT</span>
    </p>
  </div>
</header>

<section class="band showcase" style="border-top:0">
  <div class="wrap">
    <div class="showcase-bar">
      <div role="tablist" aria-label="Showcase">
        <button role="tab" aria-selected="true" aria-controls="s-form" id="tab-form">Forms</button>
        <button role="tab" aria-selected="false" aria-controls="s-data" id="tab-data">Data</button>
        <button role="tab" aria-selected="false" aria-controls="s-feedback" id="tab-feedback">Feedback</button>
      </div>
      <p class="dots" id="accent-dots" aria-label="Accent colour">
        <button type="button" aria-pressed="true" data-accent="indigo" aria-label="Indigo"></button>
        <button type="button" aria-pressed="false" data-accent="sage" aria-label="Sage"></button>
        <button type="button" aria-pressed="false" data-accent="clay" aria-label="Clay"></button>
        <button type="button" aria-pressed="false" data-accent="plum" aria-label="Plum"></button>
        <button type="button" aria-pressed="false" data-accent="gold" aria-label="Gold"></button>
        <button type="button" aria-pressed="false" data-accent="slate" aria-label="Slate"></button>
      </p>
    </div>

    <div class="stage" id="stage">
      <div class="stage-grid" id="s-form" role="tabpanel" aria-labelledby="tab-form">
        <div class="card">
          <header>Sign in</header>
          <form>
            <label for="s-email">Email</label>
            <input id="s-email" type="email" placeholder="you@example.com" required>
            <label for="s-pw">Password</label>
            <input id="s-pw" type="password" required>
            <p><label><input type="checkbox" checked> Remember me</label></p>
            <button class="primary" data-block>Sign in</button>
          </form>
        </div>
        <div class="card">
          <header>Settings</header>
          <p><label><input type="checkbox" role="switch" checked> Email digests</label></p>
          <p><label><input type="checkbox" role="switch"> Push notifications</label></p>
          <label for="s-freq">Frequency</label>
          <select id="s-freq"><option>Daily</option><option>Weekly</option></select>
          <label for="s-rng">Threshold</label>
          <input id="s-rng" type="range" value="60">
          <footer>Switches read as switches to assistive tech.</footer>
        </div>
        <div class="card">
          <header>Controls</header>
          <p class="row">
            <span role="group">
              <button aria-pressed="true">Day</button>
              <button aria-pressed="false">Week</button>
            </span>
          </p>
          <p class="row">
            <button class="primary">Primary</button>
            <button>Default</button>
          </p>
          <p class="row">
            <button data-variant="ghost">Ghost</button>
            <button popovertarget="s-menu">Menu ▾</button>
          </p>
          <div popover id="s-menu">
            <menu>
              <li><button>New file</button></li>
              <li><button>Duplicate</button></li>
            </menu>
          </div>
          <p class="row">
            <span class="badge">default</span>
            <span class="badge" data-tone="ok">paid</span>
            <span class="badge" data-tone="err">overdue</span>
          </p>
          <footer>No classes on any of it.</footer>
        </div>
      </div>

      <div class="stage-grid" id="s-data" role="tabpanel" aria-labelledby="tab-data" hidden>
        <div class="card"><header>Outstanding</header><h3>$48,200</h3><footer>12 invoices</footer></div>
        <div class="card" data-tone="err"><header>Overdue</header><h3>$9,400</h3><footer>3 invoices</footer></div>
        <div class="card" data-tone="ok"><header>Collected</header><h3>94%</h3><footer>up from 91%</footer></div>
        <div class="card" style="grid-column:1/-1">
          <header>Invoices</header>
          <table>
            <thead><tr><th>Invoice</th><th>Client</th><th align="right">Amount</th><th>Status</th></tr></thead>
            <tbody>
              <tr><td>INV-0231</td><td>Acme Corp</td><td align="right">$4,200.00</td><td><span class="badge" data-tone="err">overdue</span></td></tr>
              <tr><td>INV-0232</td><td>Globex</td><td align="right">$12,850.00</td><td><span class="badge" data-tone="ok">paid</span></td></tr>
              <tr><td>INV-0233</td><td>Initech</td><td align="right">$980.00</td><td><span class="badge">sent</span></td></tr>
            </tbody>
            <tfoot><tr><td colspan="2">Total</td><td align="right">$18,030.00</td><td></td></tr></tfoot>
          </table>
          <footer>A plain <code>&lt;table&gt;</code>, scrolling itself on a narrow screen.</footer>
        </div>
      </div>

      <div class="stage-grid" id="s-feedback" role="tabpanel" aria-labelledby="tab-feedback" hidden>
        <div class="card">
          <header>Alerts</header>
          <aside data-tone="ok"><strong>Payment received.</strong> INV-0232 is settled.</aside>
          <aside data-tone="warn"><strong>Three invoices overdue.</strong> Send reminders.</aside>
          <aside data-tone="err"><strong>Sync failed.</strong> Check your connection.</aside>
          <footer>One attribute retints the whole callout.</footer>
        </div>
        <div class="card">
          <header>Progress</header>
          <p class="row"><button aria-busy="true">Saving</button>
            <button data-tooltip="One attribute, no JavaScript">Tooltip</button></p>
          <label for="s-up">Uploading</label>
          <progress id="s-up" value="0.6"></progress>
          <p data-skeleton>This paragraph is a skeleton placeholder.</p>
          <footer>Spinners, tooltips and skeletons, no library.</footer>
        </div>
        <div class="card">
          <header>Dialog</header>
          <p>Opens and closes with no JavaScript at all.</p>
          <p class="row"><button data-tone="err" commandfor="s-dlg" command="show-modal">Delete invoice</button></p>
          <dialog id="s-dlg" closedby="any">
            <header><h3>Delete INV-0231?</h3></header>
            <p>This cannot be undone.</p>
            <footer>
              <button commandfor="s-dlg" command="close">Cancel</button>
              <button data-tone="err" commandfor="s-dlg" command="close">Delete</button>
            </footer>
          </dialog>
          <footer>The <code>command</code> attribute does the work.</footer>
        </div>
      </div>
    </div>
  </div>
</section>

<main>
  <section class="band" style="border-top:0">
    <div class="wrap">
      <div class="panel">
      <table class="spec">
        <tbody>
          <tr><th>Transfer size</th><td>8.7 KB <span>— fertig.min.css, gzipped</span></td></tr>
          <tr><th>Dependencies</th><td>0 <span>— no build step, no configuration</span></td></tr>
          <tr><th>Required classes</th><td>0 <span>— seven components and a layout set, all optional</span></td></tr>
          <tr><th>Contrast</th><td>AA <span>— every pair, both themes, measured</span></td></tr>
          <tr><th>Browser floor</th><td>Chrome 123, Safari 17.5, Firefox 129 <span>— anything since Aug 2024; nothing carried for the ones before</span></td></tr>
        </tbody>
      </table>
      </div>
    </div>
  </section>

  <section class="band" id="nojs">
    <div class="wrap">
      <div class="head">
        <h2>Switch JavaScript off. It still works.</h2>
        <p>Every other classless sheet that styles a <code>&lt;dialog&gt;</code>
          leaves you to write the open and close yourself. fertig leans on what
          the platform now ships — <code>popover</code>, <code>command</code>,
          anchor positioning — so these are behaviour-complete with no script at
          all. The table is an audit run in a browser with JavaScript disabled,
          not a claim.</p>
      </div>
      <div class="panel">
      <table class="spec">
        <tbody>
          <tr><th>Popover menu</th><td>works <span>— anchored to its button</span></td></tr>
          <tr><th>Dialog</th><td>works <span>— opens and closes through <code>command</code></span></td></tr>
          <tr><th>Drawer</th><td>works <span>— the same dialog, docked to an edge</span></td></tr>
          <tr><th>Toast</th><td>works <span>— a non-modal popover in the corner</span></td></tr>
          <tr><th>Tooltip</th><td>works <span>— one attribute, pure CSS</span></td></tr>
          <tr><th>Disclosure</th><td>works <span>— native <code>&lt;details&gt;</code>, animated to auto height</span></td></tr>
          <tr><th>Tabs</th><td>styled only <span>— switching panels needs about eight lines of your own</span></td></tr>
        </tbody>
      </table>
      </div>
      <p class="spaced"><small class="muted">Tabs are the one gap, and saying so
        is better than faking it: <code>aria-selected</code> has to be a real
        attribute for a screen reader to announce the right thing, and CSS
        cannot set attributes. The snippet is in
        <a href="docs.html#tabs">the docs</a>.</small></p>
    </div>
  </section>

  <section class="band band-quiet" id="why">
    <div class="wrap">
      <div class="head">
        <h2>Why another classless sheet</h2>
        <p>Most are either too plain to ship or too heavy to justify. fertig
          sits between them: opinionated enough to look finished, small enough
          that you never think about it again.</p>
      </div>
      <div class="rows">
        <div>
          <h3><svg class="icon" aria-hidden="true"><use href="icons.svg#languages"></use></svg>Bidirectional</h3>
          <p>Written in logical properties throughout, so <code>dir="rtl"</code>
            mirrors the whole sheet — Persian and Arabic get the same layout,
            not a patched one. Verified by rendering a Persian page in both
            directions.</p>
        </div>
        <div>
          <h3><svg class="icon" aria-hidden="true"><use href="icons.svg#file-code"></use></svg>Classless</h3>
          <p>Semantic tags are styled directly. Seven optional classes and a
            set of Tailwind-named layout utilities exist if you want them —
            you can ignore every one and the page still looks finished.</p>
        </div>
        <div>
          <h3><svg class="icon" aria-hidden="true"><use href="icons.svg#zap"></use></svg>Modern only</h3>
          <p>OKLCH, <code>light-dark()</code>, cascade layers, anchor
            positioning, container queries, <code>@property</code>. No vendor
            prefixes except the three no engine has replaced.</p>
        </div>
        <div>
          <h3><svg class="icon" aria-hidden="true"><use href="icons.svg#accessibility"></use></svg>Accessible</h3>
          <p>Every colour pair the sheet uses clears WCAG AA in both themes,
            at rest and on hover — measured, not assumed. The tightest pairing
            is the warn tone on paper at 4.80:1.</p>
        </div>
        <div>
          <h3><svg class="icon" aria-hidden="true"><use href="icons.svg#layers"></use></svg>Overridable</h3>
          <p>The sheet is wrapped in <code>@layer fertig</code>, so your own
            unlayered CSS wins at any specificity. There is no
            <code>!important</code> anywhere in it.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="band" id="preview">
    <div class="wrap">
      <div class="head">
        <h2>Plain HTML, finished page</h2>
        <p>The markup on the left is the whole input. The window on the right
          is what the browser renders with the sheet linked.</p>
      </div>
      <div class="cols-2">
        <pre><code class="language-html">&lt;body&gt;
  &lt;nav&gt;
    &lt;strong&gt;Ledger&lt;/strong&gt;
    &lt;ul&gt;&lt;li&gt;&lt;a href="#"&gt;Invoices&lt;/a&gt;&lt;/li&gt;&lt;/ul&gt;
  &lt;/nav&gt;
  &lt;main&gt;
    &lt;hgroup&gt;
      &lt;h3&gt;Invoices&lt;/h3&gt;
      &lt;p&gt;Three overdue, two awaiting approval.&lt;/p&gt;
    &lt;/hgroup&gt;
    &lt;p class="row"&gt;
      &lt;button class="primary"&gt;New invoice&lt;/button&gt;
      &lt;button&gt;Import&lt;/button&gt;
    &lt;/p&gt;
  &lt;/main&gt;
&lt;/body&gt;</code></pre>
        <div class="mock">
          <div class="mock-bar">Ledger</div>
          <div class="mock-body">
            <hgroup>
              <h3>Invoices</h3>
              <p>Three overdue, two awaiting approval.</p>
            </hgroup>
            <p class="row">
              <button class="primary">New invoice</button>
              <button>Import</button>
            </p>
            <table>
              <thead><tr><th>Client</th><th align="right">Amount</th><th>Status</th></tr></thead>
              <tbody>
                <tr><td>Acme</td><td align="right">$4,200.00</td><td><span class="badge" data-tone="err">overdue</span></td></tr>
                <tr><td>Globex</td><td align="right">$12,850.00</td><td><span class="badge" data-tone="ok">paid</span></td></tr>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="band band-quiet" id="components">
    <div class="wrap">
      <div class="head">
        <h2>A component layer, addressed by ARIA</h2>
        <p>Tabs, switches, menus and segmented controls are styled through the
          attributes that make them accessible in the first place — so a
          component can only look right if it is also announced right.</p>
      </div>
      <div class="cols-2">
        <div class="card">
          <header>Segmented control &amp; tabs</header>
          <p class="row">
            <span role="group">
              <button aria-pressed="true">Day</button>
              <button aria-pressed="false">Week</button>
              <button aria-pressed="false">Month</button>
            </span>
          </p>
          <div role="tablist">
            <button role="tab" aria-selected="true">Preview</button>
            <button role="tab" aria-selected="false">Code</button>
          </div>
          <footer><code>role="group"</code> · <code>role="tablist"</code></footer>
        </div>

        <div class="card">
          <header>Switches &amp; menus</header>
          <p><label><input type="checkbox" role="switch" checked> Run migrations</label></p>
          <p class="row">
            <button popovertarget="menu">Menu ▾</button>
            <button data-tooltip="One attribute, no JavaScript">Tooltip</button>
            <button aria-busy="true">Saving</button>
          </p>
          <div popover id="menu">
            <menu>
              <li><button>New file</button></li>
              <li><button>Duplicate</button></li>
              <li><button>Rename…</button></li>
            </menu>
          </div>
          <footer><code>role="switch"</code> · <code>[popover]</code></footer>
        </div>
      </div>
      <p class="spaced"><a href="blocks.html">Browse all sixteen blocks →</a></p>
    </div>
  </section>

  <section class="band" id="palette">
    <div class="wrap">
      <div class="head">
        <h2>A palette that stays quiet</h2>
        <p>Six hues, five steps each, in OKLCH so a step means the same thing
          in every hue. Chroma is deliberately low — these are colours meant
          to sit under text all day, not to shout from a marketing page.</p>
      </div>
      <div class="panel">
      <div class="swatches">
        <div class="ramp"><b>stone</b>
          <span data-step="100" style="background:var(--fertig-stone-100)"></span>
          <span data-step="300" style="background:var(--fertig-stone-300)"></span>
          <span data-step="500" style="background:var(--fertig-stone-500)"></span>
          <span data-step="700" style="background:var(--fertig-stone-700)"></span>
          <span data-step="900" style="background:var(--fertig-stone-900)"></span></div>
        <div class="ramp"><b>sage</b>
          <span data-step="100" style="background:var(--fertig-sage-100)"></span>
          <span data-step="300" style="background:var(--fertig-sage-300)"></span>
          <span data-step="500" style="background:var(--fertig-sage-500)"></span>
          <span data-step="700" style="background:var(--fertig-sage-700)"></span>
          <span data-step="900" style="background:var(--fertig-sage-900)"></span></div>
        <div class="ramp"><b>sky</b>
          <span data-step="100" style="background:var(--fertig-sky-100)"></span>
          <span data-step="300" style="background:var(--fertig-sky-300)"></span>
          <span data-step="500" style="background:var(--fertig-sky-500)"></span>
          <span data-step="700" style="background:var(--fertig-sky-700)"></span>
          <span data-step="900" style="background:var(--fertig-sky-900)"></span></div>
        <div class="ramp"><b>clay</b>
          <span data-step="100" style="background:var(--fertig-clay-100)"></span>
          <span data-step="300" style="background:var(--fertig-clay-300)"></span>
          <span data-step="500" style="background:var(--fertig-clay-500)"></span>
          <span data-step="700" style="background:var(--fertig-clay-700)"></span>
          <span data-step="900" style="background:var(--fertig-clay-900)"></span></div>
        <div class="ramp"><b>plum</b>
          <span data-step="100" style="background:var(--fertig-plum-100)"></span>
          <span data-step="300" style="background:var(--fertig-plum-300)"></span>
          <span data-step="500" style="background:var(--fertig-plum-500)"></span>
          <span data-step="700" style="background:var(--fertig-plum-700)"></span>
          <span data-step="900" style="background:var(--fertig-plum-900)"></span></div>
        <div class="ramp"><b>gold</b>
          <span data-step="100" style="background:var(--fertig-gold-100)"></span>
          <span data-step="300" style="background:var(--fertig-gold-300)"></span>
          <span data-step="500" style="background:var(--fertig-gold-500)"></span>
          <span data-step="700" style="background:var(--fertig-gold-700)"></span>
          <span data-step="900" style="background:var(--fertig-gold-900)"></span></div>
      </div>
      </div>
    </div>
  </section>

  <section class="band band-quiet" id="customize">
    <div class="wrap">
      <div class="head">
        <h2>Make it yours, here</h2>
        <p>Most theming is one token. Move these and the panel beside them is
          restyled live — it is real markup with the real sheet, not a picture
          of one. The contrast of whatever accent you land on is measured as
          you go, so you cannot leave with an accent that fails AA.</p>
      </div>

      <div class="cols-2">
        <form id="tune" aria-label="Theme controls">
          <p class="eyebrow">Accent · oklch</p>
          <label for="t-l">Lightness <output for="t-l" id="o-l">48%</output></label>
          <input id="t-l" type="range" min="20" max="90" step="1" value="48">

          <label for="t-c">Chroma <output for="t-c" id="o-c">0.095</output></label>
          <input id="t-c" type="range" min="0" max="0.3" step="0.005" value="0.095">

          <label for="t-h">Hue <output for="t-h" id="o-h">250</output></label>
          <input id="t-h" type="range" min="0" max="360" step="1" value="250">

          <p class="eyebrow spaced">Shape and measure</p>
          <label for="t-r">Control radius <output for="t-r" id="o-r">6px</output></label>
          <input id="t-r" type="range" min="0" max="16" step="1" value="6">

          <label for="t-w">Measure <output for="t-w" id="o-w">38rem</output></label>
          <input id="t-w" type="range" min="28" max="60" step="1" value="38">

          <label for="t-f">Text font</label>
          <select id="t-f">
            <option value="">System stack (default)</option>
            <option value="&quot;IBM Plex Sans&quot;, sans-serif">IBM Plex Sans</option>
            <option value="Georgia, serif">Georgia</option>
            <option value="&quot;IBM Plex Mono&quot;, monospace">IBM Plex Mono</option>
          </select>

          <p class="row spaced">
            <button type="button" id="t-reset" data-variant="ghost">Reset</button>
            <button type="button" id="t-copy" class="primary">Copy the CSS</button>
          </p>
        </form>

        <div>
          <div class="mock" id="t-preview">
            <div class="mock-bar">Preview</div>
            <div class="mock-body">
              <hgroup>
                <h3>Invoices</h3>
                <p>Three overdue, two awaiting approval.</p>
              </hgroup>
              <p class="row">
                <button class="primary">New invoice</button>
                <button>Import</button>
                <span class="badge" data-tone="ok">paid</span>
              </p>
              <p><label><input type="checkbox" role="switch" checked> Send reminders</label></p>
              <label for="t-demo">Client</label>
              <input id="t-demo" type="text" value="Acme Corp">
              <p><a href="#customize">A link, in the accent</a></p>
              <progress value="0.4"></progress>
            </div>
          </div>

          <p id="t-contrast" class="spaced" role="status" aria-live="polite"></p>
          <pre><code id="t-css" class="language-css"></code></pre>
        </div>
      </div>
    </div>
  </section>

  <section class="band band-quiet" id="size">
    <div class="wrap rail">
      <p class="tag">Transfer size</p>
      <div>
        <h2>Not the smallest. The smallest that does this.</h2>
        <p class="page-intro" style="margin-bottom:1.5rem">Plenty of classless
          sheets are a fraction of this — Concrete.css is 1.2 KB — and they
          style elements and stop there. The honest comparison is against the
          two that also ship a component layer, and there fertig is the
          lightest of the three while being the only one whose components work
          without JavaScript.</p>
        <div class="bars">
          <div class="bar-row"><b>fertig.min.css</b>
            <span><span class="bar-meter" style="--len:82%"></span></span><span>8.7 KB</span></div>
          <div class="bar-row"><b>matcha.css</b>
            <span><span class="bar-meter bar-alt" style="--len:85%"></span></span><span>8.7 KB</span></div>
          <div class="bar-row"><b>Pico 2.1.1 classless</b>
            <span><span class="bar-meter bar-alt" style="--len:100%"></span></span><span>10.2 KB</span></div>
        </div>
        <p class="spaced"><small class="muted">Gzipped transfer size, measured
          with <code>gzip -9</code> on the files each package ships, on
          2026-08-31.
          <a href="https://github.com/moji2002/fertig/blob/main/docs/classless-landscape-2026.md">The
          full survey is in the repo.</a></small></p>
      </div>
    </div>
  </section>

  <section class="band band-quiet" id="questions">
    <div class="wrap rail">
      <p class="tag">Questions</p>
      <div>
        <details>
          <summary>Does it really need no classes?</summary>
          <p>None. Every element in the semantic set is styled by its tag. The
            seven classes and the layout utilities are there for the cases
            where HTML has no element for what you mean — a row of buttons, a
            card — and you can build a whole site without touching them.</p>
        </details>
        <details>
          <summary>How do I override something?</summary>
          <p>Write ordinary CSS. The sheet lives in <code>@layer fertig</code>,
            so any unlayered rule of yours wins regardless of specificity. Most
            customisation is a token: set <code>--fertig-ac</code> for the accent,
            <code>--fertig-w</code> for the measure, <code>--fertig-f</code> for the type.</p>
        </details>
        <details>
          <summary>What about older browsers?</summary>
          <p>They get plain but readable HTML. The sheet runs in any browser
            released since August 2024 — Chrome 123, Safari 17.5, Firefox 129 —
            and the only thing it carries for the ones before them is a flat
            sRGB palette, so a missing <code>light-dark()</code> cannot leave a
            page unreadable. No polyfills, no fallback build. That policy is the
            reason it is 8.7 KB rather than 20.</p>
        </details>
        <details>
          <summary>Can I change the colours?</summary>
          <p>Yes. The palette is six OKLCH ramps of five steps, and the
            semantic tokens are built from them with <code>light-dark()</code>.
            Override <code>--fertig-ac</code> alone and the buttons, focus rings,
            links, selection and caret all follow.</p>
        </details>
      </div>
    </div>
  </section>

  <section class="band" id="install">
    <div class="wrap rail">
      <p class="tag">Install</p>
      <div>
        <h2>Ship it from npm, or link it from a CDN</h2>
        <div class="cols-2 spaced">
          <div>
            <p class="eyebrow">Package</p>
            <pre><code class="language-bash">npm install fertig</code></pre>
          </div>
          <div>
            <p class="eyebrow">CDN</p>
            <pre><code class="language-html">&lt;link rel="stylesheet"
  href="https://cdn.jsdelivr.net/npm/fertig/fertig.min.css"&gt;</code></pre>
          </div>
        </div>
        <p class="spaced"><a class="primary" role="button" href="docs.html">Read the documentation<svg class="icon" aria-hidden="true"><use href="icons.svg#arrow-right"></use></svg></a></p>
      </div>
    </div>
  </section>
</main>

<footer>
  <div class="wrap">
    <img src="favicon.svg" alt="fertig" class="foot-mark" width="64" height="64">
    <div class="foot-cols">
      <div>
        <strong><svg class="icon" aria-hidden="true"><use href="icons.svg#book-open"></use></svg>Documentation</strong>
        <ul>
          <li><a href="docs.html#install">Install</a></li>
          <li><a href="docs.html#layout">Layout</a></li>
          <li><a href="docs.html#components">Components</a></li>
          <li><a href="docs.html#tokens">Tokens</a></li>
        </ul>
      </div>
      <div>
        <strong><svg class="icon" aria-hidden="true"><use href="icons.svg#blocks"></use></svg>Examples</strong>
        <ul>
          <li><a href="blocks.html"><svg class="icon" aria-hidden="true"><use href="icons.svg#blocks"></use></svg>Blocks</a></li>
        </ul>
      </div>
      <div>
        <strong><svg class="icon" aria-hidden="true"><use href="icons.svg#package"></use></svg>Project</strong>
        <ul>
          <li><a href="https://github.com/moji2002/fertig"><svg class="icon" aria-hidden="true"><use href="icons.svg#git-branch"></use></svg>Source</a></li>
          <li><a href="https://www.npmjs.com/package/fertig">npm</a></li>
          <li><a href="https://github.com/moji2002/fertig/blob/main/CHANGELOG.md">Changelog</a></li>
          <li><a href="llms.txt">llms.txt</a></li>
        </ul>
      </div>
    </div>
    <p class="foot-note">
      <span>MIT licensed</span>
      <span>v{{ site.version }}</span>
      <span>8.7 KB gzipped</span>
      <span>No dependencies</span>
    </p>
  </div>
</footer>
